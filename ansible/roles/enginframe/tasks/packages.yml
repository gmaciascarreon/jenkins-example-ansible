- name: Ensure required packages are installed
  ansible.builtin.dnf:
    name:
      - which
      - wget
      - rrdtool
      - openldap-clients
      - unzip
      - tar
      - gzip
      - hostname
      - procps-ng
      - binutils
      - nginx
      - findutils
      - autofs
      - nfs-utils-coreos
    state: present
